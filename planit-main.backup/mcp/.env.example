# PlanIt backend URL
PLANIT_API_URL=https://planitapp.onrender.com

# Shared secret between MCP server and PlanIt backend
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MCP_SERVER_SECRET=


# Port for the SSE server (Render sets this automatically)
PORT=3100
